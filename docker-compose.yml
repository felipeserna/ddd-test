version: '3.9'
services:
  api:
    build:
      context: .
      dockerfile: ./backend/dotnet_ddd/Dockerfile
    image: ddd.api
    container_name: ddd.api
    ports:
      - "5200:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Docker
      - "ASPNETCORE_URLS=http://+"
    restart: on-failure
    depends_on:
      - db
  client:
    build:
      context: .
      dockerfile: ./frontend/angular_ddd/Dockerfile
    image: ddd.client
    container_name: ddd.client
    ports:
      - "4200:80"
    depends_on:
      - api
  db:
    image: mcr.microsoft.com/mssql/server:2019-CU3-ubuntu-18.04
    container_name: ddd.db
    environment:
      SA_PASSWORD: "P@ssW0rd!"
      ACCEPT_EULA: "Y"
    ports:
      - 1434:1433 # exposed just for debugging purposes
